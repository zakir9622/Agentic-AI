torch
diffusers>=0.31.0
transformers>=4.44.0
accelerate
safetensors
gradio>=5.0.0
gradio_client>=1.0.0
spaces
Pillow
numpy
# Optional Real-ESRGAN upscaler (falls back to Lanczos if the wheel fails to build):
realesrgan; platform_system != "Darwin"
