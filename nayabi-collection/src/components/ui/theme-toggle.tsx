// Deprecated: the standalone dark/light toggle was replaced by <SettingsMenu />
// (Auto / Light / Dark, defaulting to time-of-day "Auto"). Theme state now lives
// in `@/components/theme/theme`. This stub remains only to avoid stale imports.
export {};
